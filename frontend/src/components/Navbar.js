import React from "react";
import "../styles/home.css";

const Navbar = () => (
  <nav className="navbar">
    <div className="navbar-logo">🌟</div>
    <div className="navbar-title">StoreRate</div>
  </nav>
);

export default Navbar;
